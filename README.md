## Проект: Автоматический мониторинг и автопостинг мемов в Telegram

Этот проект автоматически:
- Мониторит указанные Telegram-каналы и копирует их посты в отложку вашего канала
- Ищет похожие мемы на Reddit (русскоязычные сабреддиты) с фильтрацией по модели вкуса
- Отправляет найденный контент в отложку канала с заданным интервалом

### Быстрый старт

**Автономный режим (рекомендуется):**

```bash
# Показать все доступные команды
make help

# Полная настройка проекта
make setup

# Запустить автономный режим (работает постоянно, сам ищет и отправляет мемы)
make auto
```

Автономный режим будет:
- Каждый час мониторить указанные Telegram-каналы и копировать новые посты
- Искать новые мемы с Reddit
- Фильтровать их по модели вкуса
- Автоматически отправлять всё в отложку канала
- Работать до остановки (Ctrl+C)

**Ручной режим (если нужно контролировать процесс):**

```bash
# Поиск мемов с Reddit (фильтрация по модели вкуса)
make fetch-reddit LIMIT=50

# Отправка в отложку
make schedule MAX=10 INTERVAL=2 DELAY=10
```

**Полный пайплайн (если нужно построить индекс для поиска похожих):**

```bash
# Полный пайплайн: экспорт → эмбеддинги → поиск → отправка
make pipeline-and-schedule
```

### Основные компоненты
- `config.py` — загрузка настроек и токенов из переменных окружения.
- `telegram_export.py` — экспорт постов из Telegram-канала.
- `telegram_monitor.py` — мониторинг других Telegram-каналов и копирование их постов.
- `embeddings.py` — расчёт эмбеддингов (векторных представлений) для мемов.
- `taste_model.py` — мультимодальная модель "вкуса" мемов (SigLIP + SBERT).
- `sources_reddit.py` — получение мемов с Reddit с фильтрацией по модели вкуса.
- `scheduler.py` — логика выбора мемов и отправки в отложку через аккаунт.
- `sent_tracker.py` — отслеживание уже отправленных мемов (предотвращение дубликатов).
- `auto_mode.py` — автономный режим работы (автоматический мониторинг, поиск и отправка).
- `main.py` — точка входа, запуск отдельных команд.

### Как это работает
- **Мониторинг Telegram-каналов**: `telegram_monitor.py` периодически проверяет указанные публичные каналы и копирует новые посты (только медиа: фото-мемы или видео-мемы) в отложку вашего канала. **Фильтрует посты по модели вкуса** - отправляются только подходящие мемы. Текст из постов не копируется, только медиа. Отслеживает уже обработанные сообщения в `data/processed_messages.json`.
- **Модель вкуса** (основной фильтр для Reddit): `taste_model.py` использует обученную мультимодальную модель (`mean_taste_multimodal.npy`) для оценки похожести мемов:
  - SigLIP для анализа изображений
  - SBERT для анализа текста (включая OCR)
  - Объединённый вектор "вкуса" вашего канала
  - ✅ **Это основной механизм фильтрации** - мемы отбираются по похожести на ваш стиль
- **Экспорт** (опционально): ваш аккаунт Telegram (через `Telethon`) выгружает посты канала и сохраняет медиа + подписи в `data/media`.
- **Эмбеддинги** (опционально): `embeddings.py` считает векторные представления картинок (CLIP) и строит FAISS‑индекс по всем мемам канала (для дополнительного поиска похожих).
- **Поиск кандидатов с Reddit**:
  - `sources_reddit.py` берёт мемы с русскоязычного сабреддита Reddit (по умолчанию `Pikabu`),
  - **фильтрует их по модели вкуса** (отбирает только похожие) - это основной фильтр,
  - если индекс построен, дополнительно находит самые похожие мемы в вашем канале.
- **Отправка**: `scheduler.py` через `Telethon` отправляет выбранные кандидаты в отложку канала с заданным интервалом.
- **Защита от дубликатов**: `sent_tracker.py` отслеживает уже отправленные мемы (по ID или URL) и предотвращает повторную отправку. История сохраняется в `data/sent_memes.json`.

### Установка

#### Быстрый старт (с Makefile)

```bash
# Полная настройка проекта (создание venv + установка зависимостей)
make setup

# Проверить статус проекта
make status

# Показать все доступные команды
make help
```

#### Ручная установка

1. Создайте виртуальное окружение (если ещё не создано):
   ```bash
   python3 -m venv venv
   ```

2. Активируйте виртуальное окружение:
   ```bash
   source venv/bin/activate
   ```
   После активации в начале строки терминала появится `(venv)`.

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   
   **Важно:** 
   - При первой установке EasyOCR загрузит модели для OCR (это может занять несколько минут).
   - Убедитесь, что установлена библиотека `sentencepiece` (требуется для модели вкуса SigLIP).

4. (Опционально) Для деактивации виртуального окружения после работы:
   ```bash
   deactivate
   ```

5. Создайте файл `.env` в корне проекта и укажите в нём:
   ```env
   TELEGRAM_API_ID=ваш_api_id_от_my.telegram.org
   TELEGRAM_API_HASH=ваш_api_hash_от_my.telegram.org
   TELEGRAM_CHANNEL_USERNAME=@имя_вашего_канала

   # Список каналов для мониторинга (через запятую, или оставьте пустым для дефолтных)
   SOURCE_TELEGRAM_CHANNELS=@why4ch,@beobanka,@saved_2_0,@profunctor_io,@subject282,@poieajpoij,@takoi_sebe_channel,@reptilememe,@aftertitles,@Katzen_und_Politik,@transhumemism,@bogmoegopadika

   # опционально
   REDDIT_SUBREDDIT=Pikabu  # Русскоязычный сабреддит (по умолчанию Pikabu)
   MEDIA_DIR=data/media
   EMBEDDINGS_DIR=data/embeddings
   DB_PATH=data/memes.db
   ```

### Пошаговый запуск

**Важно:** 
- Если у вас уже есть файл `mean_taste_multimodal.npy` с обученной моделью вкуса, шаги 1-2 (экспорт и построение индекса) **не обязательны**.
- Модель вкуса будет использоваться для фильтрации мемов.
- Построение индекса нужно только если вы хотите искать похожие мемы в базе канала (опционально).

**Если используете Makefile, виртуальное окружение активируется автоматически. Иначе убедитесь, что оно активировано (`source venv/bin/activate`).**

#### С Makefile (рекомендуется)

**Минимальный пайплайн (только с моделью вкуса):**
```bash
# 1. Поиск мемов с Reddit (использует модель вкуса)
make fetch-reddit LIMIT=50

# 2. Отправка в отложку
make schedule MAX=10 INTERVAL=2 DELAY=10
```

**Полный пайплайн (с построением индекса для поиска похожих):**
```bash
# 1. Экспорт постов из канала (опционально)
make export
# или с ограничением
make export-limit LIMIT=500

# 2. Построение эмбеддингов (опционально)
make build

# 3. Поиск мемов с Reddit
make fetch-reddit LIMIT=50

# 4. Отправка в отложку
make schedule MAX=10 INTERVAL=2 DELAY=10

# Или всё сразу
make pipeline-and-schedule
```

#### Без Makefile (через python3)

- **1. Экспорт истории канала (ОПЦИОНАЛЬНО)**

  ⚠️ **Этот шаг не обязателен**, если у вас уже есть файл `mean_taste_multimodal.npy` с обученной моделью вкуса.
  
  Экспорт нужен только если вы хотите:
  - Построить индекс для поиска похожих мемов в базе канала
  - Переобучить модель вкуса на новых данных
  
  Скачиваем посты и медиа из Telegram‑канала:
  
  **С Makefile:**
  ```bash
  make export
  # или с ограничением
  make export-limit LIMIT=500
  ```
  
  **Без Makefile:**
  ```bash
  python3 main.py export_telegram
  # можно ограничить количество экспортируемых сообщений:
  # EXPORT_LIMIT=500 python3 main.py export_telegram
  ```
  
  Медиа и подписи будут сохранены в `data/media`.

- **2. Построение эмбеддингов и индекса (ОПЦИОНАЛЬНО)**

  ⚠️ **Этот шаг не обязателен**, если у вас уже есть файл `mean_taste_multimodal.npy`.
  
  Индекс нужен только для поиска похожих мемов в базе канала. Основная фильтрация происходит по модели вкуса.
  
  Строим эмбеддинги (CLIP) и FAISS‑индекс по мемам канала:
  
  **С Makefile:**
  ```bash
  make build
  ```
  
  **Без Makefile:**
  ```bash
  python3 main.py build_embeddings
  ```
  
  В результате появятся файлы:
  - `data/embeddings/index.faiss`
  - `data/embeddings/meta.json`

- **3. Поиск кандидатов с Reddit (ОСНОВНОЙ ШАГ)**

  ✅ **Это основной шаг** - получаем мемы с Reddit и фильтруем их по модели вкуса.
  
  **С Makefile:**
  ```bash
  make fetch-reddit LIMIT=50
  # или без фильтрации по вкусу
  make fetch-reddit-no-taste LIMIT=50
  ```
  
  **Без Makefile:**
  ```bash
  python3 main.py fetch_reddit --limit 50
  ```
  
  Программа автоматически использует **модель вкуса** (`mean_taste_multimodal.npy`) для фильтрации мемов:
  - Мемы оцениваются по мультимодальной модели (SigLIP + SBERT)
  - Используется OCR для извлечения текста из картинок
  - Отбираются только мемы, похожие на ваш "вкус" (по умолчанию порог 0.6)
  - Если индекс построен, дополнительно ищутся похожие мемы в базе канала
  
  Параметры:
  - `--limit 50` — сколько постов обрабатывать
  - `--taste-threshold 0.6` — порог похожести для модели вкуса (0.0-1.0)
  - `--no-taste` — отключить фильтрацию по модели вкуса
  
  Кандидаты (картинка Reddit + информация о похожести) сохраняются в:
  - `data/reddit_candidates.json`
  
  **По умолчанию используется русскоязычный сабреддит `Pikabu`.** 
  Чтобы изменить сабреддит, укажите в `.env`:
  ```env
  REDDIT_SUBREDDIT=название_сабреддита
  ```

- **4. Отправка кандидатов в канал (в отложку)**

  **Автономный режим (рекомендуется):**
  
  Запускает бота в автономном режиме - он сам периодически ищет мемы и отправляет их:
  
  **С Makefile:**
  ```bash
  make auto
  ```
  
  **Без Makefile:**
  ```bash
  python3 main.py auto
  ```
  
  Параметры автономного режима:
  - `--search-interval 1.0` — интервал между поисками мемов в часах (по умолчанию 1.0)
  - `--posts-per-search 50` — сколько постов обрабатывать за один поиск (по умолчанию 50)
  - `--posts-to-schedule 10` — сколько мемов отправлять в отложку за раз (по умолчанию 10)
  - `--schedule-interval 2.0` — интервал между постами в отложке в часах (по умолчанию 2.0)
  - `--schedule-delay 10` — задержка перед отправкой первого поста в минутах (по умолчанию 10)
  - `--taste-threshold 0.6` — порог похожести для модели вкуса (по умолчанию 0.6)
  
  **Ручной режим:**
  
  Отправляем найденные кандидаты в ваш канал через аккаунт Telegram **в отложку**:
  
  **С Makefile:**
  ```bash
  make schedule MAX=10 INTERVAL=2 DELAY=10
  ```
  
  **Без Makefile:**
  ```bash
  python3 main.py schedule_posts --max 10 --interval 2 --delay 10
  ```
  
  Параметры:
  - `--max 10` / `MAX=10` — максимум мемов для отправки (по умолчанию 10)
  - `--interval 2` / `INTERVAL=2` — интервал между постами в часах (по умолчанию 1 час)
  - `--delay 10` / `DELAY=10` — задержка перед отправкой первого поста в минутах (по умолчанию 5 минут)
  
  Пример: команда выше отправит 10 мемов в отложку, первый пост через 10 минут, следующие каждые 2 часа.

### Примеры использования Makefile

```bash
# Показать все доступные команды
make help

# Полная настройка проекта
make setup

# Проверить статус (наличие .env, модели, индекса)
make status

# 🚀 АВТОНОМНЫЙ РЕЖИМ (рекомендуется)
make auto  # Работает постоянно, сам ищет и отправляет мемы

# Настройка автономного режима:
make auto SEARCH_INTERVAL=2 POSTS_PER_SEARCH=100 POSTS_TO_SCHEDULE=20

# Ручной режим (по шагам):
make export          # Экспорт постов
make build           # Построение индекса
make fetch-reddit LIMIT=100  # Поиск мемов
make schedule MAX=20 INTERVAL=2 DELAY=10  # Отправка в отложку

# Полный пайплайн: экспорт → эмбеддинги → поиск → отправка
make pipeline-and-schedule

# Очистка
make clean           # Удалить временные файлы
make clean-data      # Удалить все данные (с подтверждением)
```

### Важно
- При первом запуске скриптов, использующих `Telethon` (`export_telegram`, `schedule_posts`, `auto`), библиотека запросит код подтверждения входа в Telegram — это нужно сделать один раз.
- **Модель вкуса**: Убедитесь, что файл `mean_taste_multimodal.npy` находится в корне проекта. Этот файл содержит обученный вектор "вкуса" вашего канала.
- **Автономный режим**: Бот работает постоянно до остановки (Ctrl+C). Все действия логируются в файл `auto_mode.log`.
- При первом использовании EasyOCR загрузит модели для распознавания текста (русский и английский языки).
- По умолчанию программа ищет мемы в русскоязычном сабреддите `Pikabu`. Чтобы изменить, укажите `REDDIT_SUBREDDIT` в `.env`.
- **Мониторинг Telegram-каналов**: По умолчанию программа мониторит 12 каналов из списка. Все посты **фильтруются по модели вкуса** - отправляются только подходящие мемы. Копируются только медиа (фото-мемы или видео-мемы), текст из постов не копируется. Чтобы изменить список каналов, укажите `SOURCE_TELEGRAM_CHANNELS` в `.env` (каналы через запятую). Если переменная не указана, используются дефолтные каналы.
- **Защита от дубликатов**: Программа автоматически отслеживает уже отправленные мемы и обработанные сообщения из Telegram-каналов. История хранится в `data/sent_memes.json` и `data/processed_messages.json`. Если нужно очистить историю и начать заново, удалите эти файлы.
- Для работы в фоновом режиме (например, на сервере) используйте `nohup` или `screen`/`tmux`:
  ```bash
  nohup make auto > bot.log 2>&1 &
  # или
  screen -S memebot
  make auto
  # Ctrl+A, затем D для отсоединения
  ```

